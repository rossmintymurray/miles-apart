
<nav class="navbar navbar-default fhmm" role="navigation">
                        
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#defaultmenu">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
    </div>
            
    <!-- Collect the nav links, forms, and other content for toggling - this will be collapsed on mobile devices-->
    <div class="collapse navbar-collapse" id="defaultmenu">
        <ul class="nav navbar-nav navbar-left" id="hover_intent">
            <!-- list elements -->

            {# Set the colour to 0 #}
            {% set colour_number = 1 %} 
            {# Get the main categories and display #}
			{% for entity in entities %}
			    {% if entity.categorytype.categorytypename is sameas ("Main") %}

			    	{# Show the main category item #}
			    	<li class="dropdown fhmm-fw"><a href="{{ path('miles_apart_public_shop', { 'main_category': entity.CategorySlug }) }}" data-toggle="dropdown" class="dropdown-toggle colour_{{ colour_number }}">{{ entity.categoryname }}</a>
			    		{% set colour_number = colour_number + 1 %}

			    		<ul class="dropdown-menu fullwidth">
			    			<li class="fhmm-content withdesc">
                                <div class="row">

					    			{# Get the subcategory items #}
					    			{% for sub_entity in entities %}
					    				{% if sub_entity.categorytype.categorytypename is sameas ("Sub") and sub_entity.parent.id is sameas (entity.id) %}

					    					{# Show the sub category item #}
			    					
	                                        {% if sub_entity.categoryname is sameas ("Puzzles By Picture") %} 
	                                        	<div class="col-sm-6">
	                                        		<h3 class="title"><a href="{{ path('miles_apart_public_shop', { 'sub_category': sub_entity.categoryslug, 'main_category': entity.categoryslug }) }}">{{ sub_entity.categoryname }}</a></h3>
		                                            
		                                            <ul>

			                                            {# Create new row for multiple puzzles by picture categories #}
			                                            <div class="row">
			                                            	<div class="col-sm-4">

			                                            	

				                                            	{% set specific_count = 0 %}
				                                            	{# Get the specific category items #}
				                                            	{% for specific_entity in entities %}
				                                            		{% if specific_entity.categorytype.categorytypename is sameas ("Specific") and specific_entity.parent.id is sameas (sub_entity.id) %}

				                                            			{# Show the specific category item #}
				                                            			<li><a href="{{ path('miles_apart_public_shop', { 'specific_category': specific_entity.categoryslug, 'sub_category': sub_entity.categoryslug, 'main_category': entity.categoryslug }) }}">{{ specific_entity.categoryname }}</a></li>

				                                            			{% set specific_count = specific_count + 1 %}


				                                            			{# Close the row if 6th item #}
				                                            			{% if specific_count == 6 %}
					                                            			{% set specific_count = 0 %}
					                                            			
						                                            			
						                                        			</div>
						                                        		
				                                            				<div class="col-sm-4">	

			                                            				{% endif %}


				                                            		{% endif %}
				                                            	{% endfor %}
				                                            </div>
				                                        </div>
		                                            </ul>
		                                        </div>
	                                        
	                                        {# Show the sub category item #}
			    					
	                                        {% elseif sub_entity.categoryname is sameas ("Party Accessories") %} 
	                                        	<div class="col-sm-4">
	                                        		<h3 class="title"><a href="{{ path('miles_apart_public_shop', { 'sub_category': sub_entity.categoryslug, 'main_category': entity.categoryslug }) }}">{{ sub_entity.categoryname }}</a></h3>
		                                            <ul>
			                                            {# Create new row for multiple puzzles by picture categories #}
			                                            <div class="row">
			                                            	<div class="col-sm-6">

			                                            	

				                                            	{% set specific_count = 0 %}
				                                            	{# Get the specific category items #}
				                                            	{% for specific_entity in entities %}
				                                            		{% if specific_entity.categorytype.categorytypename is sameas ("Specific") and specific_entity.parent.id is sameas (sub_entity.id) %}

				                                            			{# Show the specific category item #}
				                                            			<li><a href="{{ path('miles_apart_public_shop', { 'specific_category': specific_entity.categoryslug, 'sub_category': sub_entity.categoryslug, 'main_category': entity.categoryslug }) }}">{{ specific_entity.categoryname }}</a></li>

				                                            			{% set specific_count = specific_count + 1 %}


				                                            			{# Close the row if 6th item #}
				                                            			{% if specific_count == 6 %}
					                                            			{% set specific_count = 0 %}
					                                            			
						                                            			
						                                        			</div>
						                                        		
				                                            				<div class="col-sm-6">	

			                                            				{% endif %}


				                                            		{% endif %}
				                                            	{% endfor %}
				                                            </div>
			                                            </div>
		                                            </ul>
		                                        </div>
	                                        {% else %}
	                                        	<div class="col-sm-2">
	                                        
		                                            <h3 class="title"><a href="{{ path('miles_apart_public_shop', { 'sub_category': sub_entity.categoryslug, 'main_category': entity.categoryslug }) }}">{{ sub_entity.categoryname }}</a></h3>
		                                            <ul>

		                                            	{# Get the specific category items #}
		                                            	{% for specific_entity in entities %}
		                                            		{% if specific_entity.categorytype.categorytypename is sameas ("Specific") and specific_entity.parent.id is sameas (sub_entity.id) %}

		                                            			{# Show the specific category item #}
		                                            			<li><a href="{{ path('miles_apart_public_shop', { 'specific_category': specific_entity.categoryslug, 'sub_category': sub_entity.categoryslug, 'main_category': entity.categoryslug }) }}">{{ specific_entity.categoryname }}</a></li>

		                                            		{% endif %}
		                                            	{% endfor %}
		                                            </ul>
		                                        </div>
	                                        {% endif %}

                                        {% endif %}

		                			{% endfor %}

		                		</div><!-- end row -->

		                	</li><!-- end grid demo -->

		                </ul><!-- end drop down menu -->
                    
                    </li><!-- end list elements -->

                {% endif %}

		    {% endfor %}

		</ul>
    </div>

{# End of navigation #}
</nav>

               