{# src/MilesApart/PublicBundle/Resources/views/about_page_layout.html.twig #}
{% extends '::public_base_foundation.html.twig' %}

{% block body %}
    
    {# Create the main body wrapper #}
    <div  class="row show-for-medium-up">
        <div class="large-12 columns">
            {% block breadcrumb %}
                {{ wo_render_breadcrumbs() }}
            {% endblock %}
        </div>
    </div>


    {# Main content row #}
    <div class="row">

        {# Create the about text column #}
        <div class="large-12 columns" >

            <!--Info box area placeholder for success/failure messages  (flash messages) -->
            {% for flashMessage in app.session.flashbag.get('public-error') %}
                <div class="flashbag">
                    <div data-alert class="alert-box alert radius">
                        {{ flashMessage }}
                        <a href="#" class="close">&times;</a>
                    </div>
                </div>
            {% endfor %}

            {# Flash message for success #}
            {% for flashMessage in app.session.flashbag.get('public-success') %}
                <div class="flashbag">
                    <div data-alert class="alert-box success radius">
                        {{ flashMessage }}
                        <a href="#" class="close">&times;</a>
                    </div>
                </div>
            {% endfor %}
            

            {# Create page content #}
            {% block page_content %}

            {% endblock %}

        {# End of about us text column #}
        </div>
      
    {# End of main content row #}
    </div>

{% endblock %}


