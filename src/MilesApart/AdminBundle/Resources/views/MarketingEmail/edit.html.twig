{# src/MilesApart/AdminBundle/Resources/views/Page/Employees/all.html.twig #}
{% extends 'MilesApartAdminBundle::Page/campaign.html.twig' %}

{#Check if the form has been submitted or not and use the appropriate form template #}
{% if submitted == false %}
    {% form_theme edit_form 'MilesApartAdminBundle:Form:fields.html.twig' 'MilesApartAdminBundle:Form:field_errors.html.twig' %}
{% else %}
    {% form_theme edit_form 'MilesApartAdminBundle:Form:submitted_fields.html.twig' 'MilesApartAdminBundle:Form:field_errors.html.twig' %}
{% endif %}

{# Check if the current page is set, if not set to overview #}
{% if current_page is not defined %}
  {% set current_page = 'marketing_email' %}
{% endif %}

{# Set the title of the page displayed in the browser #}
{% block title %}
  Edit Marketing Email
{% endblock%}

{# Set the header of the page #}
{% block page_name %}
  Edit Marketing Email		
{% endblock %}   

{# Set the introduction of the page #}
{% block page_introduction %}
  Edit existing Marketing Email
{% endblock %}

{# Set the dynamic content for this template #}
{% block page_content %}

    {{ form_start(edit_form) }}
        {{ form_errors(edit_form) }}
         <fieldset>
            <legend class="text-muted">Email Details</legend>
            {{ form_row(edit_form.marketing_email_subject_line) }}
            {{ form_row(edit_form.marketing_email_name) }}
            {{ form_row(edit_form.marketing_email_description) }}
          </fieldset>
          <fieldset>
            <legend class="text-muted">Email Type</legend>
            {{ form_row(edit_form.marketing_email_type) }}
          </fieldset>
          <fieldset>
            <legend class="text-muted">Promotion</legend>
            {{ form_row(edit_form.promotion) }}
            </fieldset>
        {{ form_rest(edit_form) }}
    </form>
    {{ form_end(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('campaign_marketing-email') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
