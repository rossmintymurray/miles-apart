{# src/MilesApart/AdminBundle/Resources/views/Employees/new.html.twig #}
{% extends 'MilesApartAdminBundle::Page/campaign.html.twig' %}

{#Check if the form has been submitted or not and use the appropriate form template #}
{% if submitted == false %}
    {% form_theme form 'MilesApartAdminBundle:Form:fields.html.twig' 'MilesApartAdminBundle:Form:field_errors.html.twig' %}
{% else %}
    {% form_theme form 'MilesApartAdminBundle:Form:submitted_fields.html.twig' 'MilesApartAdminBundle:Form:field_errors.html.twig' %}
{% endif %}

{# Check if the current page is set, if not set to overview #}
{% if current_page is not defined %}
  {% set current_page = 'new_vanity_url_promotion' %}
{% endif %}

{# Set the title of the page displayed in the browser #}
{% block title %}
	New Vanity URL Promotion
{% endblock%}

{# Set the header of the page #}
{% block page_name %}
	New Vanity URL Promotion		
{% endblock %}   

{# Set the introduction of the page #}
{% block page_introduction %}
   	Complete the form below to add a new Vanity URL Promotion to the system
{% endblock %}

{% block page_content -%}

    {{ form_start(form) }}
        {{ form_errors(form) }}
         <fieldset>
            <legend class="text-muted">Landing Page</legend>
            {{ form_row(form.vanity_url_promotion_landing_page) }}
          </fieldset>
          <fieldset>
            <legend class="text-muted">Dates Active</legend>
            {{ form_row(form.vanity_url_promotion_date_required) }}
            {{ form_row(form.vanity_url_promotion_date_to_expire) }}
          </fieldset>
          <fieldset>
            <legend class="text-muted">Vanity URL</legend>
            {{ form_row(form.vanity_url) }}
          </fieldset>
          <fieldset>
            <legend class="text-muted">Promotion</legend>
            {{ form_row(form.promotion) }}
            </fieldset>
        {{ form_rest(form) }}
    </form>
    {{ form_end(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('campaign_vanity-url-promotion') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
