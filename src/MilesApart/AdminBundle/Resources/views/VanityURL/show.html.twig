{# src/MilesApart/AdminBundle/Resources/views/Employees/show.html.twig #}
{% extends 'MilesApartAdminBundle::Page/campaign.html.twig' %}

{# Set  current page for interface highlighting #}
{% set current_page = 'vanity_url' %}

{# Set the page title #}
{% block title %}
    {{ entity.id }} - Vanity URL
{% endblock%}

{# Set the page header #}
{% block page_name %}
    {{ entity.id }} - Vanity URL           
{% endblock %}   

{# Set the page sub header #}
{% block page_introduction %}
    View Vanity URL details
{% endblock %}

{% block page_content -%}

    <table class=\"table table-hover table-bordered table-striped main_content_table show_table\">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Vanity url date created</th>
                <td>{{ entity.vanityurldatecreated|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Vanity url append text</th>
                <td>{{ entity.vanityurlappendtext }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('campaign_vanity-url') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('campaign_vanity-url_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
