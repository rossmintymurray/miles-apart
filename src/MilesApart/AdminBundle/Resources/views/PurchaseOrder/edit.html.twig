{# src/MilesApart/AdminBundle/Resources/views/Page/Employees/all.html.twig #}
{% extends 'MilesApartAdminBundle::Page/procurement.html.twig' %}

{#Check if the form has been submitted or not and use the appropriate form template #}
{% if submitted == false %}
    {% form_theme edit_form 'MilesApartAdminBundle:Form:fields.html.twig' 'MilesApartAdminBundle:Form:field_errors.html.twig' %}
{% else %}
    {% form_theme edit_form 'MilesApartAdminBundle:Form:submitted_fields.html.twig' 'MilesApartAdminBundle:Form:field_errors.html.twig' %}
{% endif %}

{# Check if the current page is set, if not set to overview #}
{% if current_page is not defined %}
  {% set current_page = 'purchase_order' %}
{% endif %}

{# Set the title of the page displayed in the browser #}
{% block title %}
  Edit Purchase Order
{% endblock%}

{# Set the header of the page #}
{% block page_name %}
  Edit Purchase Order		
{% endblock %}   

{# Set the introduction of the page #}
{% block page_introduction %}
  Edit existing Purchase Order
{% endblock %}

{# Set the dynamic content for this template #}
{% block page_content %}
    
    {{ form_start(edit_form) }}
        {{ form_errors(edit_form) }}
         <fieldset>
            <legend class="text-muted">Purchase Order Details</legend>
            {{ form_row(edit_form.supplier_purchase_order_code) }}
            {{ form_row(edit_form.purchase_order_date_completed) }}
          </fieldset>
          <fieldset>
            <legend class="text-muted">Supplier</legend>
            {{ form_row(edit_form.supplier) }}
            </fieldset>
          <fieldset>
            <legend class="text-muted">Order State</legend>
            {{ form_row(edit_form.purchase_order_state) }}
            </fieldset>
        {{ form_rest(edit_form) }}
    </form>
    {{ form_end(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('procurement_purchase-order') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
