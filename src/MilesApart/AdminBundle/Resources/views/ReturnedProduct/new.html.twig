{# src/MilesApart/AdminBundle/Resources/views/Employees/new.html.twig #}
{% extends 'MilesApartAdminBundle::Page/customerorder.html.twig' %}

{#Check if the form has been submitted or not and use the appropriate form template #}
{% if submitted == false %}
    {% form_theme form 'MilesApartAdminBundle:Form:fields.html.twig' 'MilesApartAdminBundle:Form:field_errors.html.twig' %}
{% else %}
    {% form_theme form 'MilesApartAdminBundle:Form:submitted_fields.html.twig' 'MilesApartAdminBundle:Form:field_errors.html.twig' %}
{% endif %}

{# Check if the current page is set, if not set to overview #}
{% if current_page is not defined %}
  {% set current_page = 'new_returned_product' %}
{% endif %}

{# Set the title of the page displayed in the browser #}
{% block title %}
	New Returned Product
{% endblock%}

{# Set the header of the page #}
{% block page_name %}
	New Returned Product		
{% endblock %}   

{# Set the introduction of the page #}
{% block page_introduction %}
   	Complete the form below to add a new Returned Product to the system
{% endblock %}

{% block page_content -%}

    {{ form_start(form) }}
        {{ form_errors(form) }}
         <fieldset>
            <legend class="text-muted">Return Details</legend>
            {{ form_row(form.return_processed) }}
            {{ form_row(form.return_notes) }}
            {{ form_row(form.customer_order_product) }}
            {{ form_row(form.returned_reason) }}
            </fieldset>
        {{ form_rest(form) }}
    </form>
    {{ form_end(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('customer-order_returned-product') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
